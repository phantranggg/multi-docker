sudo: required
services:
  - docker

before_install:
  - docker build -t phantrang/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true phantrang/react-test npm test

after_success:
  - docker build -t phantrang/multi-client ./client
  - docker build -t phantrang/multi-nginx ./nginx
  - docker build -t phantrang/multi-server ./server
  - docker build -t phantrang/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push phantrang/multi-client
  - docker push phantrang/multi-nginx
  - docker push phantrang/multi-server
  - docker push phantrang/multi-worker

deploy:
  edge: true
  provider: elasticbeanstalk
  region: ap-southeast-1
  app: multi-docker-bs
  env: MultiDockerBs-env
  bucket_name: elasticbeanstalk-ap-southeast-1-278603739114
  bucket_path: docker-multi
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: $AWS_SECRET_KEY
